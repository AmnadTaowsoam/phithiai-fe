import { LiffShell } from '@/components/LiffShell';

export default function LiffStatusPage() {
  return (
    <LiffShell>
      <h1 style={{ margin: '8px 0' }}>Booking status</h1>
      <p style={{ opacity: 0.8 }}>Check booking status from inside LINE (integration-ready).</p>
    </LiffShell>
  );
}

