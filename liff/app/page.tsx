import { LiffShell } from '@/components/LiffShell';

export default function LiffHomePage() {
  return (
    <LiffShell>
      <h1 style={{ margin: '8px 0' }}>Quick actions</h1>
      <p style={{ opacity: 0.8 }}>
        Use LIFF for quick booking nudges, payment shortcuts, and notifications inside LINE.
      </p>
    </LiffShell>
  );
}

